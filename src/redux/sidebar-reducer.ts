let initialState = {};

export type InititialStateType = typeof initialState;

const sidebarReducer = (state = initialState, action: any): InititialStateType => {
  return state;
}

export default sidebarReducer;